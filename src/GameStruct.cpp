#include <string>
#include <vector>

using namespace std;

// struct to store game information

struct GameStruct {
    string title;
    string platform;
    double rating;
    vector<string> genre;
};